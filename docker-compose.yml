services:
  gateway:
    build: .
    container_name: gateway
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:alpine
    container_name: gateway-redis
    ports:
      - "6379:6379"
